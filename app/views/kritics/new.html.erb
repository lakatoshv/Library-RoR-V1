<% if session[:lang] == "ua" 
    text ={
      :h1 => "Додати нову критику",
      :back => "Назад"
    } 
  elsif session[:lang] == "en" 
    text ={
      :h1 => "Add new kritics",
      :back => "Back"
    } 
  elsif session[:lang] == "ru"
    text ={
      :h1 => "Добавить новую критику",
      :back => "Назад"
    } 
  end 
%>
<div class="back">
  <h1 class="col-md-6 col-md-offset-3 text-center back"><%= text[:h1] %></h1>

  <%= render 'form', kritic: @kritic %>
  <%= link_to text[:back], kritics_path %>
</div>
