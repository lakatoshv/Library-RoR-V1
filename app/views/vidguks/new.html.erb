<% if session[:lang] == "ua" 
    text ={
      :h1 => "Додати новий відгук",
      :back => "Назад"
    } 
  elsif session[:lang] == "en" 
    text ={
      :h1 => "Add new review",
      :back => "Back"
    } 
  elsif session[:lang] == "ru"
    text ={
      :h1 => "Добавить новой отзив",
      :back => "Назад"
    } 
  end 
%>
<div class="back">
  <h1 class="col-md-6 col-md-offset-3 text-center back"><%= text[:h1] %></h1>

  <%= render 'form', vidguk: @vidguk %>
  <%= link_to text[:back], vidguks_path %>
</div>